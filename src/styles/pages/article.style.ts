import {type SxProps} from "@mui/system";
import {theme} from "@styles/theme.styles";

const commentForm = {
    maxWidth: 500,
    alignSelf: 'center',
    width: '100%'
} as const;

export const style = {
    commentForm
}
